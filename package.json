{
  "name": "A2",
  "private": true,
  "workspaces": [
    "artifacts/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @workspace/api-server --filter @workspace/staff-portal run --parallel dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "drizzle-kit": "^0.30.0"
  }
}